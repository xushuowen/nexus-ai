version: '3.8'

services:
  nexus:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - nexus-data:/app/data
    restart: unless-stopped

volumes:
  nexus-data:
