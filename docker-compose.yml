version: '3.8'

services:
  nexus:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    env_file:
      - .env
    volumes:
      - nexus-data:/app/data
    restart: unless-stopped

volumes:
  nexus-data:
